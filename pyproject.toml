[project]
name = "scan_me"
authors = [
    { name = "Tushar Patel", email = "ptusharwrk139@gmail.com"}
]
description = "QR and Barcode Scanner"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    # "frappe~=15.0.0" # Installed and managed by bench.
    "qrcode~=8.2",
    "python-barcode~=0.16.1",
    # Post-install: chromium-headless-shell auto-downloads via after_install hook (~110MB)
    # to {bench_path}/playwright-browsers/. Manual fallback in DOCUMENTATION.md.
    "playwright~=1.47",
    "pypdf~=6.1.3",
    # PAdES digital signatures — Adobe-compatible green banner (self-signed → yellow).
    "pyHanko~=0.25",
    "cryptography>=41",
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=14.0.0,<=17.0.0-dev"

# These dependencies are only installed when developer mode is enabled
[tool.bench.dev-dependencies]
# package_name = "~=1.1.0"

[tool.ruff]
line-length = 110
target-version = "py310"

[tool.ruff.lint]
select = [
    "F",
    "E",
    "W",
    "I",
    "UP",
    "B",
    "RUF",
]
ignore = [
    "B017", # assertRaises(Exception) - should be more specific
    "B018", # useless expression, not assigned to anything
    "B023", # function doesn't bind loop variable - will have last iteration's value
    "B904", # raise inside except without from
    "E101", # indentation contains mixed spaces and tabs
    "E402", # module level import not at top of file
    "E501", # line too long
    "E741", # ambiguous variable name
    "F401", # "unused" imports
    "F403", # can't detect undefined names from * import
    "F405", # can't detect undefined names from * import
    "F722", # syntax error in forward type annotation
    "W191", # indentation contains tabs
]
typing-modules = ["frappe.types.DF"]

[tool.ruff.format]
quote-style = "double"
indent-style = "tab"
docstring-code-format = true

[project.urls]
Repository = "https://github.com/Tusharp21/scan_me.git"